'use strict';self.addEventListener('push',e=>{let d={title:'Mountain Transporte',body:'Hay una actualización de tu ruta.'};try{d={...d,...e.data.json()};}catch{}e.waitUntil(self.registration.showNotification(d.title,{body:d.body,icon:'../web/icon.svg',badge:'../web/icon.svg',tag:d.event_id||'mcs-route',renotify:false,data:{url:'./'}}));});self.addEventListener('notificationclick',e=>{e.notification.close();e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if(c.url.includes('/transporte/app/'))return c.focus();}return clients.openWindow('./');}));});