(()=>{'use strict';
const URL='https://kbtjkjdjvkorekzzhxcx.supabase.co',KEY='sb_publishable_4uce_DqQ7LpVOSnGAGnfmA_ZXEUWofq';
const sb=window.supabase?.createClient(URL,KEY),$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const isAdmin=()=>($('#userChip')?.textContent||'').toLowerCase().includes('admin');
const isStaff=()=>{const t=($('#userChip')?.textContent||'').toLowerCase();return t.includes('admin')||t.includes('teacher')||t.includes('maestra')};
function modal(html){const m=$('#modal'),s=$('#sheet');if(!m||!s)return;s.innerHTML=html;m.classList.add('show')}
async function classrooms(){const {data,error}=await sb.from('mf_classrooms').select('id,name,level').eq('active',true).order('name');if(error)throw error;return data||[]}
async function studentsPanel(){if(!isAdmin())return;const rooms=await classrooms();modal('<div class="head"><div><h3>Estudiantes</h3><p>Alta, edición y asignación de grado</p></div><button class="btn soft" data-close>Cerrar</button></div><button id="stNew" class="btn primary">Agregar estudiante</button><div id="stList" class="list mt"><div class="notice">Cargando…</div></div>');$('#stNew').onclick=()=>studentForm(null,rooms);const {data,error}=await sb.from('mf_students').select('*,mf_classrooms(level,name)').order('first_name');if(error)return $('#stList').textContent=error.message;$('#stList').innerHTML=(data||[]).map(s=>`<div class="card"><div class="row"><div style="flex:1"><b>${esc(s.first_name+' '+s.last_name)}</b><small class="meta">${esc(s.mf_classrooms?.level||s.mf_classrooms?.name||'Sin grado')} ${s.date_of_birth?'· '+esc(s.date_of_birth):''}</small></div><span class="pill">${s.active?'Activo':'Inactivo'}</span></div><div class="row mt"><button class="btn soft" data-stedit="${s.id}">Editar</button></div></div>`).join('')||'<div class="notice">No hay estudiantes registrados.</div>';$$('[data-stedit]').forEach(b=>{const s=(data||[]).find(x=>x.id===b.dataset.stedit);b.onclick=()=>studentForm(s,rooms)})}
function studentForm(s,rooms){modal(`<div class="head"><div><h3>${s?'Editar':'Agregar'} estudiante</h3><p>Mountain Family</p></div><button class="btn soft" data-close>Cerrar</button></div><div class="field"><label>NOMBRE</label><input id="stFirst" value="${esc(s?.first_name||'')}"></div><div class="field"><label>APELLIDO</label><input id="stLast" value="${esc(s?.last_name||'')}"></div><div class="field"><label>FECHA DE NACIMIENTO</label><input id="stDob" type="date" value="${esc(s?.date_of_birth||'')}"></div><div class="field"><label>GRADO</label><select id="stRoom"><option value="">Sin asignar</option>${rooms.map(c=>`<option value="${c.id}" ${s?.classroom_id===c.id?'selected':''}>${esc(c.level||c.name)}</option>`).join('')}</select></div><div class="field"><label><input id="stDiaper" type="checkbox" ${s?.uses_diaper?'checked':''}> Usa pañal</label></div><div class="field"><label>IDIOMA FAMILIAR</label><input id="stLang" value="${esc(s?.family_language||'es')}"></div><div class="field"><label>ALERGIAS</label><textarea id="stAllergies" rows="2">${esc(s?.allergies||'')}</textarea></div><div class="field"><label>RESTRICCIONES ALIMENTARIAS</label><textarea id="stFood" rows="2">${esc(s?.food_restrictions||'')}</textarea></div><div class="field"><label><input id="stActive" type="checkbox" ${s?.active!==false?'checked':''}> Estudiante activo</label></div><button id="stSave" class="btn primary mt">Guardar estudiante</button>`);$('#stSave').onclick=async()=>{const first=$('#stFirst').value.trim();if(!first)return alert('Escribe el nombre.');const payload={first_name:first,last_name:$('#stLast').value.trim(),date_of_birth:$('#stDob').value||null,classroom_id:$('#stRoom').value||null,uses_diaper:$('#stDiaper').checked,family_language:$('#stLang').value.trim()||'es',allergies:$('#stAllergies').value.trim()||null,food_restrictions:$('#stFood').value.trim()||null,active:$('#stActive').checked};const q=s?sb.from('mf_students').update(payload).eq('id',s.id):sb.from('mf_students').insert(payload);const {error}=await q;if(error)return alert(error.message);await studentsPanel()}}
async function plansPanel(e){if(!isStaff())return;e?.preventDefault();e?.stopImmediatePropagation();const rooms=await classrooms();const {data,error}=await sb.from('mf_lesson_plans').select('*,mf_classrooms(name,level)').order('plan_date',{ascending:false}).limit(100);if(error)return alert(error.message);modal(`<div class="head"><div><h3>Planificación académica</h3><p>CRECE + Currículo + Block Building</p></div><button class="btn soft" data-close>Cerrar</button></div><button id="lpNew" class="btn primary">Nueva planificación</button><div class="list mt">${(data||[]).map(x=>`<div class="card"><b>${esc(x.title)}</b><small class="meta">${esc(x.mf_classrooms?.level||'General')} · ${esc(x.plan_date)} · ${esc(x.status)}</small><p>${esc(x.theme||'')}</p></div>`).join('')||'<div class="notice">No hay planificaciones.</div>'}</div>`);$('#lpNew').onclick=()=>planForm(rooms)}
function planForm(rooms){const today=new Date().toISOString().slice(0,10);modal(`<div class="head"><h3>Nueva planificación</h3><button class="btn soft" data-close>Cerrar</button></div><div class="field"><label>GRADO</label><select id="lpRoom"><option value="">General</option>${rooms.map(c=>`<option value="${c.id}">${esc(c.level||c.name)}</option>`).join('')}</select></div><div class="field"><label>FECHA</label><input id="lpDate" type="date" value="${today}"></div><div class="field"><label>TÍTULO</label><input id="lpTitle"></div><div class="field"><label>TEMA</label><textarea id="lpTheme" rows="4"></textarea></div><div class="field"><label>ESTADO</label><select id="lpStatus"><option value="draft">Borrador</option><option value="published">Publicada</option></select></div><button id="lpSave" class="btn primary mt">Guardar planificación</button>`);$('#lpSave').onclick=async()=>{const title=$('#lpTitle').value.trim();if(!title)return alert('Escribe un título.');const {data:{user}}=await sb.auth.getUser();const {error}=await sb.from('mf_lesson_plans').insert({classroom_id:$('#lpRoom').value||null,plan_date:$('#lpDate').value,title,theme:$('#lpTheme').value.trim()||null,status:$('#lpStatus').value,created_by:user?.id||null});if(error)return alert(error.message);await plansPanel({preventDefault(){},stopImmediatePropagation(){}})}}
function install(){const home=$('#home');if(home)new MutationObserver(()=>{if(!isAdmin())return;const quick=[...home.querySelectorAll('.quick')].find(q=>q.querySelector('[data-action="broadcast"]'));if(quick&&!quick.querySelector('[data-admin-students]')){const b=document.createElement('button');b.dataset.adminStudents='1';b.innerHTML='<span>🎓</span><b>Estudiantes</b>';b.onclick=studentsPanel;quick.prepend(b)}}).observe(home,{childList:true,subtree:true});document.addEventListener('click',e=>{const b=e.target.closest?.('[data-action="plans"]');if(b&&isStaff())plansPanel(e)},true)}
document.addEventListener('DOMContentLoaded',install);window.MountainStudents=studentsPanel;
})();